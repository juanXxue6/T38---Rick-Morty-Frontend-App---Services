import { RemoveBlankSpacePipe } from './remove-blank-space.pipe';

describe('RemoveBlankSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveBlankSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
